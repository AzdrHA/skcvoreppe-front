# Getting Started

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run start:dev
$ npm run start:prod

# build project
$ npm run build:dev
$ npm run buid:prod
```
