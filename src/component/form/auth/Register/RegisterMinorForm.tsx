import React, {FC} from 'react';

type RegisterMinorFormProps = {
  register: any;
  formState: any;
}

export const RegisterMinorForm: FC<RegisterMinorFormProps> = (props: RegisterMinorFormProps) => {
  return (
    <div>

    </div>
  );
};
